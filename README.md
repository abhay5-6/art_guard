# Art Watcher

A background desktop tool that watches Krita illustration folders,
automatically exports `.kra` files to `.jpg`, renames them intelligently,
and stores both formats separately.

## Features
- Folder change detection
- Krita `.kra` → `.jpg` export
- Smart naming (ML-ready)
- Auto-start on boot (Windows)

## Status
🚧 In development
